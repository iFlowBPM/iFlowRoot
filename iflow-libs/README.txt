iFlow uses some infosistema libraries. Here they are.

These libraries should be installed in local maven repositories according to instructions in LIBRARY_MANAGEMENT.txt
in the main project dir.

